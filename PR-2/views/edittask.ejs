<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    body {
        font-family: "Poppins", sans-serif;
        background: #eef2f7;
        margin: 0;
        padding: 30px 0;
    }

    p {
        font-size: 24px;
        font-weight: 600;
        color: #333;
        text-align: center;
        margin-bottom: 25px;
    }

    /* --- FORM CARD --- */
    form {
        background: #ffffff;
        padding: 30px;
        width: 360px;
        border-radius: 14px;
        box-shadow: 0 8px 20px rgba(0,0,0,0.10);
        display: inline-block;
        text-align: left;
        border-top: 5px solid #4a90e2;
    }

    form tr td {
        display: block;
        padding: 6px 0;
        font-size: 15px;
        color: #444;
        font-weight: 500;
    }

    /* INPUT FIELDS */
    input[type="text"],
    input[type="date"],
    select {
        width: 100%;
        padding: 12px;
        border-radius: 8px;
        border: 1px solid #cfd2d8;
        background: #f7f9fc;
        outline: none;
        font-size: 14px;
        margin-top: 4px;
        transition: 0.3s ease;
    }

    input[type="text"]:focus,
    input[type="date"]:focus,
    select:focus {
        border-color: #4a90e2;
        background: #ffffff;
        box-shadow: 0 0 6px rgba(74,144,226,0.4);
    }

    /* DISABLED ID FIELD */
    input[disabled] {
        background: #ececec;
        cursor: not-allowed;
    }

    /* SUBMIT BUTTON */
    input[type="submit"] {
        margin-top: 16px;
        width: 100%;
        padding: 12px;
        border-radius: 8px;
        border: none;
        background: #4a90e2;
        color: white;
        font-size: 16px;
        font-weight: 600;
        cursor: pointer;
        transition: 0.3s ease;
    }

    input[type="submit"]:hover {
        background: #357abd;
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.15);
    }
</style>

<body>
    <p style="text-align: center;"> To do-list form</p>

    <center>
        <form action="/updatetask?Taskid=<%= Task.id%> " method="post">
            <tr>
                <td>id:</td>
                <td><input type="text" name="id" value="<%=Task.id %>" disabled></td>
            </tr>
            <br>
            <br>
            <tr>
                <td>Task Title</td>
                <td><input type="text" name="Title" value="<%=Task.Title %>"></td>
            </tr>
            <br>
            <br>
            <tr>
                <td>Task Description</td>
                <td><input type="text" name="Description" value="<%=Task.Description %>"></td>
            </tr>
            <br>
            <br>
            <tr>
                <td>Task Priority</td>
                <td>
                    <select name="Priority">
                        <option value="High" <%=Task.Priority=="High" ? "selected" : "" %>>High</option>
                        <option value="Medium" <%=Task.Priority=="Medium" ? "selected" : "" %>>Medium</option>
                        <option value="Low" <%=Task.Priority=="Low" ? "selected" : "" %>>Low</option>
                    </select>

                </td>
            </tr>

            <br>
            <br>
            <tr>
                <td>Due Date</td>
                <td><input type="date" name="DueDate" value="<%=Task.DueDate %>"></td>
            </tr>
            <br>
            <br>
            <tr>
                <td></td>
                <td><input type="submit" value="submit"></td>
            </tr>
            <br>
            <br>
        </form>
    </center>

</body>

</html>